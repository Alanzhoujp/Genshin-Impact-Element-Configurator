import java.awt.Color;
import java.awt.Dimension;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import java.awt.Font;
import javax.swing.ButtonGroup;
import javax.swing.ImageIcon;
import javax.swing.JButton;
import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JOptionPane;
import javax.swing.JPanel;
import javax.swing.JRadioButton;

public class MyFrame extends JFrame implements ActionListener {
  private static final long serialVersionUID = 1L;
  boolean elementalReaction, elementalResonance;
  ButtonGroup setA, setB;
  Color deepBlue;
  ImageIcon genshinImpactIcon, anemoIcon, cryoIcon, dendroIcon, electroIcon, geoIcon, hydroIcon, pyroIcon;
  JButton reset;
  JLabel promptA, promptB, textA, textB, textC, textD, textE,
  burning, crystallizeCryo, crystallizeElectro, crystallizeHydro, crystallizePyro, electroCharged,
  frozen, melt, overloaded, superconduct, swirlCryo, swirlElectro, swirlHydro, swirlPyro, vaporize,
  enduringRock, ferventFlames, highVoltage, impetuousWinds, protectiveCanopy, shatteringIce, soothingWaters;
  JPanel panel;
  JRadioButton anemoA, cryoA, dendroA, electroA, geoA, hydroA, pyroA,
  anemoB, cryoB, dendroB, electroB, geoB, hydroB, pyroB;
  MyFrame() {
    setA = new ButtonGroup();
    setB = new ButtonGroup();
    deepBlue = new Color(7, 42, 108);
    genshinImpactIcon = new ImageIcon("genshinImpactIcon.png");
    anemoIcon = new ImageIcon("anemoIcon.png");
    cryoIcon = new ImageIcon("cryoIcon.png");
    dendroIcon = new ImageIcon("dendroIcon.png");
    electroIcon = new ImageIcon("electroIcon.png");
    geoIcon = new ImageIcon("geoIcon.png");
    hydroIcon = new ImageIcon("hydroIcon.png");
    pyroIcon = new ImageIcon("pyroIcon.png");
    reset = new JButton("RESET");
    reset.addActionListener(this);
    reset.setFont(new Font("Arial", Font.PLAIN, 30));
    reset.setFocusable(false);
    reset.setPreferredSize(new Dimension(200, 50));
    promptA = new JLabel("Please select an element from set A:");
    promptA.setFont(new Font("Times New Roman", Font.BOLD, 30));
    promptA.setForeground(Color.WHITE);
    promptB = new JLabel("Please select an element from set B:");
    promptB.setFont(new Font("Times New Roman", Font.BOLD, 30));
    promptB.setForeground(Color.WHITE);
    textA = new JLabel();
    textB = new JLabel();
    textC = new JLabel();
    textD = new JLabel();
    textE = new JLabel();
    burning = new JLabel(new ImageIcon());
    crystallizeCryo = new JLabel(new ImageIcon());
    crystallizeElectro = new JLabel(new ImageIcon());
    crystallizeHydro = new JLabel(new ImageIcon());
    crystallizePyro = new JLabel(new ImageIcon());
    electroCharged = new JLabel(new ImageIcon());
    frozen = new JLabel(new ImageIcon());
    melt = new JLabel(new ImageIcon());
    overloaded = new JLabel(new ImageIcon());
    superconduct = new JLabel(new ImageIcon());
    swirlCryo = new JLabel(new ImageIcon());
    swirlElectro = new JLabel(new ImageIcon());
    swirlHydro = new JLabel(new ImageIcon());
    swirlPyro = new JLabel(new ImageIcon());
    vaporize = new JLabel(new ImageIcon());
    enduringRock = new JLabel(new ImageIcon());
    ferventFlames = new JLabel(new ImageIcon());
    highVoltage = new JLabel(new ImageIcon());
    impetuousWinds = new JLabel(new ImageIcon());
    protectiveCanopy = new JLabel(new ImageIcon());
    shatteringIce = new JLabel(new ImageIcon());
    soothingWaters = new JLabel(new ImageIcon());
    panel = new JPanel();
    anemoA = new JRadioButton("Anemo");
    anemoA.addActionListener(this);
    anemoA.setFocusable(false);
    anemoA.setIcon(anemoIcon);
    cryoA = new JRadioButton("Cryo");
    cryoA.addActionListener(this);
    cryoA.setFocusable(false);
    cryoA.setIcon(cryoIcon);
    dendroA = new JRadioButton("Dendro");
    dendroA.addActionListener(this);
    dendroA.setFocusable(false);
    dendroA.setIcon(dendroIcon);
    electroA = new JRadioButton("Electro");
    electroA.addActionListener(this);
    electroA.setFocusable(false);
    electroA.setIcon(electroIcon);
    geoA = new JRadioButton("Geo");
    geoA.addActionListener(this);
    geoA.setFocusable(false);
    geoA.setIcon(geoIcon);
    hydroA = new JRadioButton("Hydro");
    hydroA.addActionListener(this);
    hydroA.setFocusable(false);
    hydroA.setIcon(hydroIcon);
    pyroA = new JRadioButton("Pyro");
    pyroA.addActionListener(this);
    pyroA.setFocusable(false);
    pyroA.setIcon(pyroIcon);
    setA.add(anemoA);
    setA.add(cryoA);
    setA.add(dendroA);
    setA.add(electroA);
    setA.add(geoA);
    setA.add(hydroA);
    setA.add(pyroA);

    anemoB = new JRadioButton("Anemo");
    anemoB.addActionListener(this);
    anemoB.setFocusable(false);
    anemoB.setIcon(anemoIcon);
    cryoB = new JRadioButton("Cryo");
    cryoB.addActionListener(this);
    cryoB.setFocusable(false);
    cryoB.setIcon(cryoIcon);
    dendroB = new JRadioButton("Dendro");
    dendroB.addActionListener(this);
    dendroB.setFocusable(false);
    dendroB.setIcon(dendroIcon);
    electroB = new JRadioButton("Electro");
    electroB.addActionListener(this);
    electroB.setFocusable(false);
    electroB.setIcon(electroIcon);
    geoB = new JRadioButton("Geo");
    geoB.addActionListener(this);
    geoB.setFocusable(false);
    geoB.setIcon(geoIcon);
    hydroB = new JRadioButton("Hydro");
    hydroB.addActionListener(this);
    hydroB.setFocusable(false);
    hydroB.setIcon(hydroIcon);
    pyroB = new JRadioButton("Pyro");
    pyroB.addActionListener(this);
    pyroB.setFocusable(false);
    pyroB.setIcon(pyroIcon);
    setB.add(anemoB);
    setB.add(cryoB);
    setB.add(dendroB);
    setB.add(electroB);
    setB.add(geoB);
    setB.add(hydroB);
    setB.add(pyroB);
    panel.add(promptA);
    panel.add(anemoA);
    panel.add(cryoA);
    panel.add(dendroA);
    panel.add(electroA);
    panel.add(geoA);
    panel.add(hydroA);
    panel.add(pyroA);
    panel.add(promptB);
    panel.add(anemoB);
    panel.add(cryoB);
    panel.add(dendroB);
    panel.add(electroB);
    panel.add(geoB);
    panel.add(hydroB);
    panel.add(pyroB);
    panel.add(reset);
    panel.add(textA);
    panel.add(textB);
    panel.add(textC);
    panel.add(textD);
    panel.add(textE);
    panel.setOpaque(false);
    this.add(panel);
    this.getContentPane().setBackground(deepBlue);
    this.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    this.setIconImage(genshinImpactIcon.getImage());
    this.setSize(1100, 900);
    this.setTitle("Genshin Impact Element Configurator");
    this.setVisible(true);

  }
  public void ReactionAndResonance() {
    if (elementalReaction) {
      JOptionPane.showMessageDialog(null, "Element A and element B cause elemental reaction.", "Genshin Impact Elemental Reaction", JOptionPane.INFORMATION_MESSAGE);
    }
    if (elementalResonance) {
      JOptionPane.showMessageDialog(null, "Element A and element B cause elemental resonance.", "Genshin Impact Elemental Resonance", JOptionPane.INFORMATION_MESSAGE);
    }
  }
  public void actionPerformed(ActionEvent e) {
    if (e.getSource() == reset) {
      setA.clearSelection();
      setB.clearSelection();
      textA.setText("");
      textB.setText("");
      textC.setText("");
      textD.setText("");
      textE.setText("");
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);
      repaint();
    }
    if ((cryoA.isSelected() && hydroB.isSelected()) || cryoB.isSelected() && hydroA.isSelected()) {
      elementalReaction = true;
      elementalResonance = false;
      ReactionAndResonance();
      textA.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textA.setForeground(Color.WHITE);
      textA.setText("When");
      textB.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textB.setForeground(Color.YELLOW);
      textB.setText("Cryo");
      textC.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textC.setForeground(Color.WHITE);
      textC.setText("meets");
      textD.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textD.setForeground(Color.YELLOW);
      textD.setText("Hydro, Frozen");
      textE.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textE.setForeground(Color.WHITE);
      textE.setText("occurs.");
      frozen = new JLabel(new ImageIcon("frozen.png"));
      panel.add(frozen);
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);
    } else if ((pyroA.isSelected() && hydroB.isSelected()) || pyroB.isSelected() && hydroA.isSelected()) {
      elementalReaction = true;
      elementalResonance = false;
      ReactionAndResonance();
      textA.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textA.setForeground(Color.WHITE);
      textA.setText("When");
      textB.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textB.setForeground(Color.YELLOW);
      textB.setText("Pyro");
      textC.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textC.setForeground(Color.WHITE);
      textC.setText("meets");
      textD.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textD.setForeground(Color.YELLOW);
      textD.setText("Hydro, Vaporize");
      textE.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textE.setForeground(Color.WHITE);
      textE.setText("occurs.");
      vaporize = new JLabel(new ImageIcon("vaporize.png"));
      panel.add(vaporize);
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);
    } else if ((electroA.isSelected() && hydroB.isSelected()) || electroB.isSelected() && hydroA.isSelected()) {
      elementalReaction = true;
      elementalResonance = false;
      ReactionAndResonance();
      textA.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textA.setForeground(Color.WHITE);
      textA.setText("When");
      textB.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textB.setForeground(Color.YELLOW);
      textB.setText("Electro");
      textC.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textC.setForeground(Color.WHITE);
      textC.setText("meets");
      textD.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textD.setForeground(Color.YELLOW);
      textD.setText("Hydro, Electro-Charged");
      textE.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textE.setForeground(Color.WHITE);
      textE.setText("occurs.");
      electroCharged = new JLabel(new ImageIcon("electroCharged.png"));
      panel.add(electroCharged);
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);
    } else if ((pyroA.isSelected() && electroB.isSelected()) || pyroB.isSelected() && electroA.isSelected()) {
      elementalReaction = true;
      elementalResonance = false;
      ReactionAndResonance();
      textA.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textA.setForeground(Color.WHITE);
      textA.setText("When");
      textB.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textB.setForeground(Color.YELLOW);
      textB.setText("Pyro");
      textC.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textC.setForeground(Color.WHITE);
      textC.setText("meets");
      textD.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textD.setForeground(Color.YELLOW);
      textD.setText("Electro, Overloaded");
      textE.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textE.setForeground(Color.WHITE);
      textE.setText("occurs.");
      overloaded = new JLabel(new ImageIcon("overloaded.png"));
      panel.add(overloaded);
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);
    } else if ((cryoA.isSelected() && electroB.isSelected()) || cryoB.isSelected() && electroA.isSelected()) {
      elementalReaction = true;
      elementalResonance = false;
      ReactionAndResonance();
      textA.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textA.setForeground(Color.WHITE);
      textA.setText("When");
      textB.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textB.setForeground(Color.YELLOW);
      textB.setText("Cryo");
      textC.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textC.setForeground(Color.WHITE);
      textC.setText("meets");
      textD.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textD.setForeground(Color.YELLOW);
      textD.setText("Electro, Superconduct");
      textE.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textE.setForeground(Color.WHITE);
      textE.setText("occurs.");
      superconduct = new JLabel(new ImageIcon("superconduct.png"));
      panel.add(superconduct);
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);
    } else if ((pyroA.isSelected() && dendroB.isSelected()) || pyroB.isSelected() && dendroA.isSelected()) {
      elementalReaction = true;
      elementalResonance = false;
      ReactionAndResonance();
      textA.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textA.setForeground(Color.WHITE);
      textA.setText("When Pyro meets Dendro, it triggers");
      textD.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textD.setForeground(Color.YELLOW);
      textD.setText("Burning.");
      burning = new JLabel(new ImageIcon("burning.png"));
      panel.add(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);
    } else if ((anemoA.isSelected() && cryoB.isSelected()) || (anemoB.isSelected() && cryoA.isSelected())) {
      elementalReaction = true;
      elementalResonance = false;
      ReactionAndResonance();
      textA.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textA.setForeground(Color.WHITE);
      textA.setText("When Anemo meets Cryo, it triggers Swirl.");
      swirlCryo = new JLabel(new ImageIcon("swirlCryo.png"));
      panel.add(swirlCryo);
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);
    } else if ((anemoA.isSelected() && electroB.isSelected()) || (anemoB.isSelected() && electroA.isSelected())) {
      elementalReaction = true;
      elementalResonance = false;
      ReactionAndResonance();
      textA.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textA.setForeground(Color.WHITE);
      textA.setText("When Anemo meets Electro, it triggers Swirl.");
      swirlElectro = new JLabel(new ImageIcon("swirlElectro.png"));
      panel.add(swirlElectro);
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);
    } else if ((anemoA.isSelected() && hydroB.isSelected()) || (anemoB.isSelected() && hydroA.isSelected())) {
      elementalReaction = true;
      elementalResonance = false;
      ReactionAndResonance();
      textA.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textA.setForeground(Color.WHITE);
      textA.setText("When Anemo meets Hydro, it triggers Swirl.");
      swirlHydro = new JLabel(new ImageIcon("swirlHydro.png"));
      panel.add(swirlHydro);
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);
    } else if ((anemoA.isSelected() && pyroB.isSelected()) || (anemoB.isSelected() && pyroA.isSelected())) {
      elementalReaction = true;
      elementalResonance = false;
      ReactionAndResonance();
      textA.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textA.setForeground(Color.WHITE);
      textA.setText("When Anemo meets Pyro, it triggers Swirl.");
      swirlPyro = new JLabel(new ImageIcon("swirlPyro.png"));
      panel.add(swirlPyro);
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);
    } else if ((cryoA.isSelected() && pyroB.isSelected()) || cryoB.isSelected() && pyroA.isSelected()) {
      elementalReaction = true;
      elementalResonance = false;
      ReactionAndResonance();
      textA.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textA.setForeground(Color.WHITE);
      textA.setText("When");
      textB.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textB.setForeground(Color.YELLOW);
      textB.setText("Cryo");
      textC.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textC.setForeground(Color.WHITE);
      textC.setText("meets");
      textD.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textD.setForeground(Color.YELLOW);
      textD.setText("Pyro, Melt");
      textE.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textE.setForeground(Color.WHITE);
      textE.setText("occurs.");
      melt = new JLabel(new ImageIcon("melt.png"));
      panel.add(melt);
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);
    } else if ((geoA.isSelected() && hydroB.isSelected()) || geoB.isSelected() && hydroA.isSelected()) {
      elementalReaction = true;
      elementalResonance = false;
      ReactionAndResonance();
      textA.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textA.setForeground(Color.WHITE);
      textA.setText("When");
      textB.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textB.setForeground(Color.YELLOW);
      textB.setText("Geo");
      textC.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textC.setForeground(Color.WHITE);
      textC.setText("comes into contact with");
      textD.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textD.setForeground(Color.YELLOW);
      textD.setText("Hydro element, Crystallize");
      textE.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textE.setForeground(Color.WHITE);
      textE.setText("will be triggered!");
      crystallizeHydro = new JLabel(new ImageIcon("crystallizeHydro.png"));
      panel.add(crystallizeHydro);
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);
    } else if ((geoA.isSelected() && pyroB.isSelected()) || geoB.isSelected() && pyroA.isSelected()) {
      elementalReaction = true;
      elementalResonance = false;
      ReactionAndResonance();
      textA.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textA.setForeground(Color.WHITE);
      textA.setText("When");
      textB.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textB.setForeground(Color.YELLOW);
      textB.setText("Geo");
      textC.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textC.setForeground(Color.WHITE);
      textC.setText("comes into contact with");
      textD.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textD.setForeground(Color.YELLOW);
      textD.setText("Pyro element, Crystallize");
      textE.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textE.setForeground(Color.WHITE);
      textE.setText("will be triggered!");
      crystallizePyro = new JLabel(new ImageIcon("crystallizePyro.png"));
      panel.add(crystallizePyro);
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);
    } else if ((geoA.isSelected() && cryoB.isSelected()) || geoB.isSelected() && cryoA.isSelected()) {
      elementalReaction = true;
      elementalResonance = false;
      ReactionAndResonance();
      textA.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textA.setForeground(Color.WHITE);
      textA.setText("When");
      textB.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textB.setForeground(Color.YELLOW);
      textB.setText("Geo");
      textC.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textC.setForeground(Color.WHITE);
      textC.setText("comes into contact with");
      textD.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textD.setForeground(Color.YELLOW);
      textD.setText("Cryo element, Crystallize");
      textE.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textE.setForeground(Color.WHITE);
      textE.setText("will be triggered!");
      crystallizeCryo = new JLabel(new ImageIcon("crystallizeCryo.png"));
      panel.add(crystallizeCryo);
      panel.remove(burning);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);
    } else if ((geoA.isSelected() && electroB.isSelected()) || geoB.isSelected() && electroA.isSelected()) {
      elementalReaction = true;
      elementalResonance = false;
      ReactionAndResonance();
      textA.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textA.setForeground(Color.WHITE);
      textA.setText("When");
      textB.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textB.setForeground(Color.YELLOW);
      textB.setText("Geo");
      textC.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textC.setForeground(Color.WHITE);
      textC.setText("comes into contact with");
      textD.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textD.setForeground(Color.YELLOW);
      textD.setText("Electro element, Crystallize");
      textE.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textE.setForeground(Color.WHITE);
      textE.setText("will be triggered!");
      crystallizeElectro = new JLabel(new ImageIcon("crystallizeElectro.png"));
      panel.add(crystallizeElectro);
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);
    } else if ((anemoA.isSelected() && anemoB.isSelected())) {
      elementalReaction = false;
      elementalResonance = true;
      ReactionAndResonance();
      textB.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textB.setForeground(Color.YELLOW);
      textB.setText("Impetuous Winds");
      textC.setFont(new Font("Times New Roman", Font.BOLD, 25));
      textC.setForeground(Color.WHITE);
      textC.setText("Decreases Stamina Consumption by 15%.");
      textD.setFont(new Font("Times New Roman", Font.BOLD, 25));
      textD.setForeground(Color.WHITE);
      textD.setText("Increases Movement SPD by 10%. Shortens Skill CD by 5%.");
      impetuousWinds = new JLabel(new ImageIcon("impetuousWinds.png"));
      panel.add(impetuousWinds);
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);
    } else if ((cryoA.isSelected() && cryoB.isSelected())) {
      elementalReaction = false;
      elementalResonance = true;
      ReactionAndResonance();
      textB.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textB.setForeground(Color.YELLOW);
      textB.setText("Shattering Ice");
      textC.setFont(new Font("Times New Roman", Font.BOLD, 25));
      textC.setForeground(Color.WHITE);
      textC.setText("Affected by Electro for 40% less time.");
      textD.setFont(new Font("Times New Roman", Font.BOLD, 25));
      textD.setForeground(Color.WHITE);
      textD.setText("Increases CRIT Rate against opponents that are Frozen or affected by Cryo by 15%.");
      shatteringIce = new JLabel(new ImageIcon("shatteringIce.png"));
      panel.add(shatteringIce);
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(soothingWaters);
    } else if ((electroA.isSelected() && electroB.isSelected())) {
      elementalReaction = false;
      elementalResonance = true;
      ReactionAndResonance();
      textB.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textB.setForeground(Color.YELLOW);
      textB.setText("High Voltage");
      textC.setFont(new Font("Times New Roman", Font.BOLD, 25));
      textC.setForeground(Color.WHITE);
      textC.setText("Affected by Hydro for 40% less time. Superconduct, Overloaded, and Electro-Charged");
      textD.setFont(new Font("Times New Roman", Font.BOLD, 25));
      textD.setForeground(Color.WHITE);
      textD.setText("have a 100% chance to generate an Electro Elemental Particle (CD:5s).");
      highVoltage = new JLabel(new ImageIcon("highVoltage.png"));
      panel.add(highVoltage);
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);

    } else if ((geoA.isSelected() && geoB.isSelected())) {
      elementalReaction = false;
      elementalResonance = true;
      ReactionAndResonance();
      textB.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textB.setForeground(Color.YELLOW);
      textB.setText("Enduring Rock");
      textC.setFont(new Font("Times New Roman", Font.BOLD, 25));
      textC.setForeground(Color.WHITE);
      textC.setText("Increases shield strength by 15%. ");
      textD.setFont(new Font("Times New Roman", Font.BOLD, 25));
      textD.setForeground(Color.WHITE);
      textD.setText("Additionally, characters protected by a shield will have the following special characteristics:");
      textE.setFont(new Font("Times New Roman", Font.BOLD, 25));
      textE.setForeground(Color.WHITE);
      textE.setText("dealt increased by 15%, dealing DMG to enemies will decrease their Geo RES by 20% for 15s");
      enduringRock = new JLabel(new ImageIcon("enduringRock.png"));
      panel.add(enduringRock);
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);
    } else if ((hydroA.isSelected() && hydroB.isSelected())) {
      elementalReaction = false;
      elementalResonance = true;
      ReactionAndResonance();
      textB.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textB.setForeground(Color.YELLOW);
      textB.setText("Soothing Water");
      textC.setFont(new Font("Times New Roman", Font.BOLD, 25));
      textC.setForeground(Color.WHITE);
      textC.setText("Affected by Pyro for 40% less time.");
      textD.setFont(new Font("Times New Roman", Font.BOLD, 25));
      textD.setForeground(Color.WHITE);
      textD.setText("Increases incoming healing by 30%.");
      soothingWaters = new JLabel(new ImageIcon("soothingWaters.png"));
      panel.add(soothingWaters);
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(ferventFlames);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
    } else if ((pyroA.isSelected() && pyroB.isSelected())) {
      elementalReaction = false;
      elementalResonance = true;
      ReactionAndResonance();
      textB.setFont(new Font("Times New Roman", Font.BOLD, 30));
      textB.setForeground(Color.YELLOW);
      textB.setText("Fervent Flames");
      textC.setFont(new Font("Times New Roman", Font.BOLD, 25));
      textC.setForeground(Color.WHITE);
      textC.setText("Affected by Cryo for 40% less time. Increases ATK by 25%.");
      ferventFlames = new JLabel(new ImageIcon("ferventFlames.png"));
      panel.add(ferventFlames);
      panel.remove(burning);
      panel.remove(crystallizeCryo);
      panel.remove(crystallizeElectro);
      panel.remove(crystallizeHydro);
      panel.remove(crystallizePyro);
      panel.remove(electroCharged);
      panel.remove(frozen);
      panel.remove(melt);
      panel.remove(overloaded);
      panel.remove(superconduct);
      panel.remove(swirlCryo);
      panel.remove(swirlElectro);
      panel.remove(swirlHydro);
      panel.remove(swirlPyro);
      panel.remove(vaporize);
      panel.remove(enduringRock);
      panel.remove(highVoltage);
      panel.remove(impetuousWinds);
      panel.remove(protectiveCanopy);
      panel.remove(shatteringIce);
      panel.remove(soothingWaters);
    } else {

    }
  }
}